{
  "name": "worldedit",
  "private": "true",
  "scripts": {
    "build": "gulp build",
    "watch": "gulp watch",
    "installmod": "gulp install",
    "uninstallmod": "gulp uninstall"
  },
  "devDependencies": {
    "minecraft-addon-toolchain": "^1.0.1",
    "minecraft-addon-toolchain-browserify": "^1.0.1"
  }
}
